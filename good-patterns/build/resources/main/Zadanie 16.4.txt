SELECT I.NAME, J.DESCRIPTION
FROM ISSUESLISTS I
JOIN ISSUES J on I.ID = J.ISSUESLIST_ID;

SELECT U.FIRSTNAME, U.LASTNAME, I.DESCRIPTION
FROM ISSUES I
JOIN USER U ON I.ID = U.ID

SELECT U.FIRSTNAME,  COUNT(*) AS TASK
FROM ISSUES I
JOIN USER U ON I.ID = U.ID
GROUP BY U.FIRSTNAME;

